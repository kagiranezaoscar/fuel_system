<x-app-layout><x-slot name="header"><h2 class="text-xl font-semibold">Purchase Fuel</h2></x-slot><div class="mx-auto max-w-4xl px-4 py-8"><form method="POST" action="{{ route('customer.purchases.store') }}" class="rounded-lg border bg-white p-6 dark:border-gray-700 dark:bg-gray-800">@csrf<select name="payment_method" required class="rounded-md border-gray-300 dark:border-gray-700 dark:bg-gray-900"><option value="mobile_money">Mobile Money</option><option value="cash">Cash</option><option value="card">Card</option><option value="bank_transfer">Bank Transfer</option></select><div class="mt-6 space-y-3" x-data="{rows:[0], add(){this.rows.push(this.rows.length)}}"><template x-for="(row,index) in rows" :key="index"><div class="grid gap-3 sm:grid-cols-2"><select :name="`items[${index}][fuel_id]`" required class="rounded-md border-gray-300 dark:border-gray-700 dark:bg-gray-900"><option value="">Fuel type</option>@foreach($fuels as $fuel)<option value="{{ $fuel->id }}">{{ $fuel->fuel_name }} - RWF {{ $fuel->price_per_liter }} ({{ $fuel->available_quantity }} L)</option>@endforeach</select><input :name="`items[${index}][liters]`" type="number" min="0.001" step="0.001" placeholder="Liters" required class="rounded-md border-gray-300 dark:border-gray-700 dark:bg-gray-900"></div></template><button type="button" @click="add" class="rounded-md border px-3 py-2 text-sm">Add line</button></div>@if($errors->any())<div class="mt-4 text-sm text-red-600">{{ $errors->first() }}</div>@endif<div class="mt-6"><button class="rounded-md bg-emerald-600 px-4 py-2 font-semibold text-white">Submit Purchase</button></div></form></div></x-app-layout>

