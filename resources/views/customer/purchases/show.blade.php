<x-app-layout><x-slot name="header"><h2 class="text-xl font-semibold">Purchase #{{ $sale->id }}</h2></x-slot><div class="mx-auto max-w-4xl px-4 py-8"><x-flash /><div class="rounded-lg border bg-white p-6 dark:border-gray-700 dark:bg-gray-800"><div class="flex justify-between"><div><p class="font-semibold">{{ $sale->sale_date->format('Y-m-d H:i') }}</p><p class="text-sm text-gray-500">{{ $sale->status }}</p></div><a class="rounded-md bg-emerald-600 px-4 py-2 text-sm font-semibold text-white" href="{{ route('customer.invoices.download', $sale) }}">Download Invoice</a></div><table class="mt-6 min-w-full text-sm"><tbody class="divide-y dark:divide-gray-700">@foreach($sale->details as $detail)<tr><td class="p-3">{{ $detail->fuel->fuel_name }}</td><td class="p-3">{{ $detail->liters }} L</td><td class="p-3">RWF {{ number_format($detail->subtotal, 2) }}</td></tr>@endforeach</tbody></table><p class="mt-4 text-right text-xl font-bold">RWF {{ number_format($sale->total_amount, 2) }}</p></div></div></x-app-layout>

