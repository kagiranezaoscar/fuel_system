<x-app-layout><x-slot name="header"><h2 class="text-xl font-semibold">Customers</h2></x-slot><div class="mx-auto max-w-7xl px-4 py-8"><x-flash /><div class="mb-4 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><form><input name="search" value="{{ request('search') }}" placeholder="Search customers..." class="rounded-md border-gray-300 dark:border-gray-700 dark:bg-gray-900"></form><a href="{{ route('manager.customers.create') }}" class="rounded-md bg-emerald-600 px-4 py-2 text-sm font-semibold text-white">Add Customer</a></div><div class="grid gap-4 md:grid-cols-3">@foreach($customers as $customer)<div class="rounded-lg border bg-white p-5 dark:border-gray-700 dark:bg-gray-800"><p class="font-semibold">{{ $customer->name }}</p><p class="text-sm text-gray-500">{{ $customer->email }}</p><p class="mt-3 text-sm">Purchases: {{ $customer->sales_count }}</p><p class="text-sm">Spent: RWF {{ number_format($customer->sales_sum_total_amount ?? 0, 2) }}</p><div class="mt-4 flex gap-3 text-sm"><a class="font-semibold text-emerald-600" href="{{ route('manager.customers.edit', $customer) }}">Edit</a><form method="POST" action="{{ route('manager.customers.destroy', $customer) }}" onsubmit="return confirm('Delete this customer?')">@csrf @method('DELETE')<button class="font-semibold text-red-600">Delete</button></form></div></div>@endforeach</div><div class="mt-4">{{ $customers->links() }}</div></div></x-app-layout>
