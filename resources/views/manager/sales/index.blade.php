<x-app-layout><x-slot name="header"><h2 class="text-xl font-semibold">Sales</h2></x-slot><div class="mx-auto max-w-7xl px-4 py-8"><x-flash /><div class="mb-4 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><form class="flex gap-2"><input name="search" value="{{ request('search') }}" placeholder="Customer" class="rounded-md border-gray-300 dark:border-gray-700 dark:bg-gray-900"><input name="from" type="date" value="{{ request('from') }}" class="rounded-md border-gray-300 dark:border-gray-700 dark:bg-gray-900"><input name="to" type="date" value="{{ request('to') }}" class="rounded-md border-gray-300 dark:border-gray-700 dark:bg-gray-900"><button class="rounded-md border px-3">Filter</button></form><a href="{{ route('manager.sales.create') }}" class="rounded-md bg-emerald-600 px-4 py-2 text-sm font-semibold text-white">Record Sale</a></div><div class="overflow-x-auto rounded-lg border bg-white dark:border-gray-700 dark:bg-gray-800"><table class="min-w-full text-sm"><thead><tr><th class="p-3 text-left">Date</th><th class="p-3 text-left">Customer</th><th class="p-3 text-left">Payment</th><th class="p-3 text-left">Status</th><th class="p-3 text-left">Total</th></tr></thead><tbody class="divide-y dark:divide-gray-700">@foreach($sales as $sale)<tr><td class="p-3"><a class="text-emerald-600" href="{{ route('manager.sales.show', $sale) }}">{{ $sale->sale_date->format('Y-m-d H:i') }}</a></td><td class="p-3">{{ $sale->customer?->name ?? 'Walk-in customer' }}</td><td class="p-3">{{ $sale->payment_method }}</td><td class="p-3">{{ $sale->status }}</td><td class="p-3 font-semibold">RWF {{ number_format($sale->total_amount, 2) }}</td></tr>@endforeach</tbody></table></div><div class="mt-4">{{ $sales->links() }}</div></div></x-app-layout>

