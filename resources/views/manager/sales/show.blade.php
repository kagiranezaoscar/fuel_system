<x-app-layout><x-slot name="header"><h2 class="text-xl font-semibold">Sale #{{ $sale->id }}</h2></x-slot><div class="mx-auto max-w-4xl px-4 py-8"><x-flash /><div class="rounded-lg border bg-white p-6 dark:border-gray-700 dark:bg-gray-800"><div class="flex justify-between"><div><p class="font-semibold">{{ $sale->customer?->name ?? 'Walk-in customer' }}</p><p class="text-sm text-gray-500">{{ $sale->sale_date->format('Y-m-d H:i') }}</p></div><div class="text-right"><p class="text-2xl font-bold">RWF {{ number_format($sale->total_amount, 2) }}</p><p class="text-sm">{{ $sale->payment_method }}</p></div></div><table class="mt-6 min-w-full text-sm"><thead><tr><th class="p-3 text-left">Fuel</th><th class="p-3 text-left">Liters</th><th class="p-3 text-left">Price</th><th class="p-3 text-left">Subtotal</th></tr></thead><tbody class="divide-y dark:divide-gray-700">@foreach($sale->details as $detail)<tr><td class="p-3">{{ $detail->fuel->fuel_name }}</td><td class="p-3">{{ $detail->liters }}</td><td class="p-3">{{ $detail->price_per_liter }}</td><td class="p-3">{{ $detail->subtotal }}</td></tr>@endforeach</tbody></table></div></div></x-app-layout>

