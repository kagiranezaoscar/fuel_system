<x-app-layout><x-slot name="header"><h2 class="text-xl font-semibold">Stock Movements</h2></x-slot><div class="mx-auto max-w-7xl px-4 py-8"><x-flash /><div class="mb-4 flex justify-end"><a href="{{ route('manager.stock.create') }}" class="rounded-md bg-emerald-600 px-4 py-2 text-sm font-semibold text-white">Record Stock</a></div><div class="overflow-x-auto rounded-lg border bg-white dark:border-gray-700 dark:bg-gray-800"><table class="min-w-full text-sm"><thead><tr><th class="p-3 text-left">Date</th><th class="p-3 text-left">Fuel</th><th class="p-3 text-left">Type</th><th class="p-3 text-left">Quantity</th><th class="p-3 text-left">Reason</th></tr></thead><tbody class="divide-y dark:divide-gray-700">@foreach($movements as $movement)<tr><td class="p-3">{{ $movement->created_at->format('Y-m-d H:i') }}</td><td class="p-3">{{ $movement->fuel->fuel_name }}</td><td class="p-3">{{ $movement->movement_type }}</td><td class="p-3">{{ $movement->quantity }} L</td><td class="p-3">{{ $movement->reason }}</td></tr>@endforeach</tbody></table></div><div class="mt-4">{{ $movements->links() }}</div></div></x-app-layout>

